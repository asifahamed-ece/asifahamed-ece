# Environment variables for the self-hosted github-readme-activity-graph deployment.
# Set these in Vercel: Project -> Settings -> Environment Variables.
# Do NOT commit real tokens.

# GitHub Personal Access Token (scopes: repo, read:user).
# Required by the activity graph to read commit contributions reliably.
TOKEN=